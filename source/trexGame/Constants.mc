module TrexGame {
    module Constants {
        const PIXELS_SPEED = 9;
        const CLOUD_PIXELS_SPEED = 1;

        const SCORE_TIMER_INTERVAL = 100;

        const START_TIMER_INTERVAL = 140;
    }
}